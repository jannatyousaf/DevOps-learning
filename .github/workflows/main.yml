name: My First Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Code lao
        uses: actions/checkout@v3
      
      - name: Hello print karo
        run: echo "Pipeline chal gayi!"
      
      - name: Date dekho
        run: date